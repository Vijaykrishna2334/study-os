import InterviewPlannerClient from "./InterviewPlannerClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Interview Planner — Study OS" };

export default function InterviewPlannerPage() {
  return <InterviewPlannerClient />;
}
